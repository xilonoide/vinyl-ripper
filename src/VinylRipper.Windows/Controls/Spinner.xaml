<UserControl x:Class="VinylRipper.Windows.Controls.Spinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="20" Height="20" IsHitTestVisible="False">
    <Grid RenderTransformOrigin="0.5,0.5">
        <Grid.RenderTransform>
            <RotateTransform />
        </Grid.RenderTransform>
        <Grid.Style>
            <Style TargetType="Grid">
                <Style.Triggers>
                    <!-- Sólo animamos mientras el spinner está visible; ahorra CPU cuando está oculto. -->
                    <DataTrigger Binding="{Binding IsVisible, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard x:Name="Spin">
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(RotateTransform.Angle)"
                                                     From="0" To="360" Duration="0:0:0.9" />
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <StopStoryboard BeginStoryboardName="Spin" />
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Ellipse Stroke="{StaticResource Brush.Border}" StrokeThickness="2.5" />
        <!-- Arco de ~270º -->
        <Path Stroke="{StaticResource Brush.Accent}" StrokeThickness="2.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
              Stretch="None" Data="M 10,1.25 A 8.75,8.75 0 1 1 1.25,10" />
    </Grid>
</UserControl>
